import React from 'react';
import "./about.scss"

const About = (props) => {
    console.log(props)
    return (
        <div className='about'>
            About thus page........
        </div>
    );
};

export default About;